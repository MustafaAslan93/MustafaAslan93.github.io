function Numbers() {
    this.numCorrect = 0;
    this.number = [];
    //initialize array with standard numbers
};
function Numbers(cookie) {
    //read cookie
    //initialize array
};
function Number(word,digit,errorNum,placed) {
    this.word = word;
    //continue here
}